language: php

matrix:
    include:
        - php: 7.0
          env: CHECKS=yes

sudo: false

cache:
    directories:
        - $HOME/.composer/cache

install:
    - /bin/true

script:
    - which phpunit
    - cat `which phpunit`
    - cat /home/travis/.phpenv/versions/7.0/bin/phpunit


